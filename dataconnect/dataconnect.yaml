specVersion: "v1"
serviceId: "mmanage"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mmanage-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
